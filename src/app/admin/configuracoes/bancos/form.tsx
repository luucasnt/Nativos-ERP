"use client";
import { useActionState } from "react";
import { createBankAccount } from "./actions";
import { buttonClass, inputClass, labelClass } from "@/lib/ui";
const initial = { error: null, saved: false };
export function BankAccountForm() { const [state, action, pending] = useActionState(createBankAccount, initial); return <form action={action} className="surface-panel grid gap-3 p-5"><h2 className="section-heading">Nova conta</h2><label><span className={labelClass}>Nome identificador *</span><input name="name" required className={inputClass} placeholder="Conta principal Nativos" /></label><label><span className={labelClass}>Tipo *</span><select name="type" className={inputClass} defaultValue="corrente"><option value="corrente">Conta corrente</option><option value="poupanca">Poupança</option><option value="digital">Conta digital</option><option value="caixa">Caixa</option></select></label><label><span className={labelClass}>Chave PIX</span><input name="pix_key" className={inputClass} /></label><label><span className={labelClass}>Saldo inicial</span><input name="initial_balance" type="number" min="0" step="0.01" defaultValue="0" className={inputClass} /></label>{state.error && <p className="text-sm text-red-700">{state.error}</p>}{state.saved && <p className="text-sm text-success">Conta criada.</p>}<button disabled={pending} className={buttonClass}>{pending ? "Salvando…" : "Cadastrar conta"}</button></form>; }
